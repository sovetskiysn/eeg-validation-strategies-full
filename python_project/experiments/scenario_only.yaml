defaults:
  - /config
  - _self_

# Run every scientific data-and-validation recipe with the base decoder from
# configs/config.yaml, without sweeping decoder options.
hydra:
  sweeper:
    params:
      +scenario: glob(*)
