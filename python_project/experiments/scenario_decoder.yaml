defaults:
  - /config
  - _self_

# BasicSweeper expands both config groups and runs their Cartesian product.
# `scenario` chooses a complete data-and-validation recipe; `decoder` chooses
# a scientifically valid representation-and-pipeline bundle.
hydra:
  sweeper:
    params:
      +scenario: glob(*)
      +decoder: glob(*)
