defaults:
  - _sam40_common_default
  - _self_

name: sam40_stroop
report_dir: _preparation/sam40_stroop

mne_bids_pipeline:
  task: [relax, stroop]
  deriv_root: ${..dataset_dir}/derivatives/mne-bids-pipeline/relax-stroop
