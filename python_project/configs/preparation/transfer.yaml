name: from_${preparation.source.name}
source: ???
targets: {}
# Expose the source preparation recipe at the ordinary path used by the
# classical feature config. Transfer validation still receives source and
# targets separately; this alias only resolves shared feature parameters.
mne_bids_pipeline: ${.source.mne_bids_pipeline}
