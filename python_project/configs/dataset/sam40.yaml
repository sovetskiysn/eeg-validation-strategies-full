name: sam40

# Полная разметка релиза (relax — один класс, три стрессовые задачи — другой)
# живёт константой DATASET_MAPPING в src/preparation.py. Здесь — плоский
# список имён, которые из неё вычитаются, и ничего больше: конфиг не может
# изменить метки или выдумать класс.
#
# Это список, а списки Hydra заменяет целиком (в отличие от словарей, которые
# она сливает), поэтому scenario-рецепт задаёт один полный состав:
#   exclude_conditions: [arithmetic, mirror]   -> relax против stroop
exclude_conditions: []
