# @package _global_

defaults:
  - /preparation@preparation.source: sam40_mirror
  - /preparation@preparation.targets.distinguishing: distinguishing
  - _self_
  - override /validation_strategy: cross_dataset
  - override /preparation: transfer
