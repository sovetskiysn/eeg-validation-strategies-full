# @package _global_

defaults:
  - override /validation_strategy: baseline
  - override /preparation: sam40

preparation:
  mne_bids_pipeline:
    task: [relax, stroop]
    deriv_root: ${..dataset_dir}/derivatives/mne-bids-pipeline/relax-stroop
