# @package pipeline_components.model
# One histogram decision stump makes this a smoke test, not an experiment.
name: xgboost_test
params:
  _target_: xgboost.XGBClassifier
  random_state: ${seed}
  tree_method: hist
  device: cuda
  n_estimators: 1
  max_depth: 1
  max_bin: 2
